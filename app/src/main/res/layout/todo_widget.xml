<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_glass_bg"
    android:padding="12dp">

    <!-- Wavy glass background image -->
    <ImageView
        android:id="@+id/widget_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />

    <!-- Header bar -->
    <RelativeLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:paddingBottom="6dp">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:text="TO DO✔️"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_streak"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:text="🔥 --"
            android:textColor="#FF9100"
            android:textSize="14sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- Empty view for list -->
    <TextView
        android:id="@+id/widget_empty_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_header"
        android:layout_above="@+id/widget_bottom_bar"
        android:gravity="center"
        android:text="No tasks for today!"
        android:textColor="#80FFFFFF"
        android:textSize="12sp"
        android:visibility="gone" />

    <!-- Scrollable ListView of Tasks of the Day -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_header"
        android:layout_above="@+id/widget_bottom_bar"
        android:divider="#1AFFFFFF"
        android:dividerHeight="1dp"
        android:nestedScrollingEnabled="true"
        android:scrollbars="vertical" />

    <!-- Bottom Actions bar -->
    <RelativeLayout
        android:id="@+id/widget_bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:paddingTop="6dp">

        <TextView
            android:id="@+id/widget_footer_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:text="Today's Schedule"
            android:textColor="#60FFFFFF"
            android:textSize="9sp" />

        <!-- Save icon on bottom right corner of widgets -->
        <ImageButton
            android:id="@+id/widget_save_button"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Add New Task"
            android:scaleType="fitCenter"
            android:padding="6dp"
            android:src="@drawable/ic_widget_save"
            android:tint="#FFFDFD" />
    </RelativeLayout>
</RelativeLayout>
